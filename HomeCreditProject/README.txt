HOME CREDIT PROJECT

1. Put all Kaggle CSV files inside data/.
2. Install dependencies: pip install -r requirements.txt
3. Train ONCE: python train_model.py
4. Saved models appear in models/.
5. Future use: load them with predict_model.py. No retraining is needed.

Important: predictions require the same feature engineering pipeline and feature columns used during training.
